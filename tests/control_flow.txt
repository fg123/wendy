WendyScript Pre-Processor Dump
let a = 10 ; 
let f => { 
  let a ; 
  pop a ; 
  if ( a < 10 ) { 
    ret a is less than 10 ; 
    } else if ( a > 10 ) { 
    ret a is greater than 10 ; 
    } else { 
    ret a is 10 ; 
    } ; 
  } ; 
push a ; 
call f ; 
let ~tmp1 ; 
pop ~tmp1 ; 
~tmp1 ; 

let dispatch => (fn, arg) native dispatch;

// Return values from these functions don't do anything
let make_ten => (x) { x * 10; }

"Hello World";
// Need a more robust IO test to test multithreaded output!
dispatch(make_ten, 10);



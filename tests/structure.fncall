WendyScript Pre-Processor Dump: Function Calls

       1: <struct> point => (x, y); 
      10: push 10; 
      13: push 40; 
      16: call point; 
      19: let ~tmp1; 
      22: pop ~tmp1; 
      25: let a = ~tmp1; 
      30: a . x; 
      34: a . y;